<%= form_for(@affiliation) do |f| %>
  <% if @affiliation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@affiliation.errors.count, "error") %> prohibited this affiliation from being saved:</h2>

      <ul>
      <% @affiliation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="select">
    <%= f.label :team_id %><br />
    <%= f.select :team_id, @teams.map{|t| [t.tname, t.id]} %>
  </div>

  <div class="select">
    <%= f.label :player_id %><br />
    <%= f.select :player_id, @players.map{|t| [t.pname, t.id]} %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
